source 'https://rubygems.org'

ruby '2.1.2'

# lightweight framework
gem 'sinatra', '~> 1.4', :require => 'sinatra/base'

gem 'localtunnel', '~>1.0'
gem 'httparty', '= 0.13.7'
gem "facebook-messenger", github: "hyperoslo/facebook-messenger", branch: "master"
gem 'tux', '0.3.0'

group :development do
  # for routing request to your localhost
  # gem 'localtunnel'
end